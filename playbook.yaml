---
- hosts: all
  become: true
  tasks:
  - name: Delete Kubernetes Objects
    command: kubectl delete -f kubernetes.yaml
    ignore_errors: True
  - name: Deploy Kubernetes Objects
    command: kubectl apply -f kubernetes.yaml
